export default class Stock {
  constructor( date, close, volume, open, high, low ) {
    this.date = date;
    this.close = close;
    this.volume = volume;
    this.open = open;
    this.high = high;
    this.low = low;
  }
}
